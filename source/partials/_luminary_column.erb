<td>
  <% content.each do |cell| %>
    <% if (cell[:image].nil?) %>
      <div class="luminary-placeholder"><%= cell[:placeholder] %></div>
    <% else %>
      <div class="luminary-placeholder" style="<%="background: url('/images/#{cell[:image]}') no-repeat #{cell[:horizontal_center] ? "center" : "left"} center" %>"></div>
      <% labelInfo = cell[:label] || "" %>
      <div><%= labelInfo %></div>
    <% end %>
  <% end %>
</td>
